# weather-cli

A tiny command-line tool that fetches the current weather for a given city. Built this over a weekend to learn the requests library properly.

## Usage

```bash
python weather.py "Muscat"
```

## TODO

- [ ] Add caching so it doesn't hit the API on every call
- [ ] Support multiple cities at once
- [ ] Clean up error handling (currently just crashes lol)
